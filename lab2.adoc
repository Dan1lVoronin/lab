= Отчет №2 - Типы данных, переменные, указатели
:toc:
:toc-title: Оглавление
:toclevels: 3
:sectnums:
:sectnumlevels: 3
:icons: font
:source-highlighter: coderay
:figure-caption: Рисунок

include::title.adoc[]

== Цель

Цель - узнать основные типы данных для микроконтроллеров, включая их размеры и особенности представления в памяти.

== Какие типы данных бывают? (Для нашего микроконтроллера)

=== Встроенные типы (целочисленные)

* bool — 1 байт — Представляет значения true или false.
* char — 1 байт — Символ ASCII, используется в C-строках или std::string без преобразования в Юникод.
* unsigned char — 1 байт — Аналог байта, с C++17 есть std::byte.
* int — 4 байта — Целочисленное значение, выбор по умолчанию для целых чисел.
* unsigned int — 4 байта — Беззнаковое целое число.
* float — 4 байта — Число с плавающей точкой, аппаратно поддерживается некоторыми микроконтроллерами.
* double — 8 байт — Число с плавающей запятой двойной точности, выбор по умолчанию для значений с плавающей точкой.

=== Модификаторы типов данных

* short int — 2 байта — Целочисленное знаковое значение укороченной длины.
* unsigned short int — 2 байта — Целочисленное беззнаковое значение укороченной длины.
* long int — 8 байт — Выбор по умолчанию для целочисленных значений, может быть длиннее int на некоторых платформах.
* unsigned long int — 8 байт — Беззнаковое целое число, может быть длиннее int на некоторых платформах.
* long double — 8 байт — Число с плавающей запятой двойной точности с увеличенной точностью.

=== Как вычисляется их размер?

Размер типа данных зависит от архитектуры процессора и компилятора. Из лекции: 

Для размеров типов существует правило:

[source,cpp]
----

1             <= sizeof(char)     <= sizeof(short) <= sizeof(int) <= sizeof(long)
1             <= sizeof(bool)     <= sizeof(long) 
sizeof(char)  <= sizeof(long)
sizeof(float) <= sizeof(double)   <= sizeof(long double)
sizeof(T)     == sizeof(signed T) == sizeof(unsigned T)
----

== Что такое неявное преобразование типа?

Неявное преобразование происходит автоматически, когда значение одного типа присваивается переменной другого типа. Например, если int преобразуется в float без явного указания программиста.

== Что такое явное преобразование типа?

Явное преобразование требует, чтобы программист сам указал, в какой тип преобразовать значение. Для этого используются операторы преобразования, такие как *static_cast*, *reinterpret_cast*, *dynamic_cast*, *const_cast*. Нужно, чтобы предотвратить нежелательные потери данных или неожиданное поведение программы.

== Когда используется static_cast?

Этот оператор используется для безопасного преобразования между совместимыми типами, например, из int в double.

== Когда используется reinterpret_cast?

Этот оператор позволяет интерпретировать один тип как другой, даже если они несовместимы. Например, можно преобразовать *int* в *char*. Однако такое преобразование может привести к ошибкам, если программист неправильно интерпретирует данные.

== Почему преобразование одного типа в другой может быть проблемой?

Преобразование типов может привести к потере данных (например, при преобразовании double в int дробная часть теряется) и неопределенному поведению программы (особенно при использовании reinterpret_cast). 

== Что такое указатель?

Указатель — это переменная, содержащая адрес другой переменной.

== Операции над указателем

Над ним можно выполнять следующие операции:

=== Присваивание адреса:

[source,cpp]
----
int x = 5;
int* p = &x;  // p указывает на x
----

=== Разыменовывание (`*`) — получение значения по адресу:

[source,cpp]
----
int y = *p;   // y = 5
----

=== Взятие адреса переменной (`&`):

[source,cpp]
----
int* q = &x;  // q хранит адрес x
----

=== Арифметика указателей:

[source,cpp]
----
int arr[] = {10, 20, 30};
int* ptr = arr;
ptr++;  // теперь указывает на arr[1]
----

=== Сравнение указателей:

[source,cpp]
----
if (ptr1 == ptr2) { ... }
----

=== Присваивание `nullptr`:

[source,cpp]
----
int* p = nullptr;
----

== Константный указатель и указатель на константу

Эти два понятия часто путают. Разница:

=== Указатель на константу (`const int* p` или `int const* p`)

* Нельзя изменять значение по адресу
* Можно изменять сам указатель

[source,cpp]
----
const int* p = &x;   // или: int const* p = &x;
*p = 10;             // ошибка
p = &y;              // можно
----

=== Константный указатель (`int* const p = &x`)

* Можно изменять значение по адресу
* Нельзя изменять указатель

[source,cpp]
----
int* const p = &x;
*p = 20;             // можно
p = &y;              // ошибка
----

=== Константный указатель на константу (`const int* const p = &x`)

* Нельзя ни изменять значение, ни указатель

[source,cpp]
----
const int* const p = &x;
*p = 20;             // ошибка
p = &y;              // ошибка
----

== Разыменовывание указателя

Разыменование — доступ к значению по адресу:

[source,cpp]
----
int x = 5;
int* p = &x;
int y = *p;  // y = 5
----

== Операция взятия адреса

Получение адреса переменной:

[source,cpp]
----
int x = 10;
int* p = &x;  // p хранит адрес x
----

== Сводная таблица

[options="header"]
|===
| Операция               | Описание
| `*p`                   | Разыменование — доступ к значению
| `&x`                   | Взятие адреса переменной
| `const int* p`         | Указатель на константу
| `int* const p`         | Константный указатель
| `const int* const p`   | Константный указатель на константу
| `p++`, `p--`, `p + n`  | Арифметика указателей
|===

=== Почему нельзя вычитать один указатель из другого, если они указывают на разные объекты?

Разность указателей имеет смысл только в пределах одного массива или объекта. Если вычесть указатели, принадлежащие разным объектам, результат будет неопределенным, так как они могут находиться в совершенно разных частях памяти.

== Как установить бит?

Можно использовать *побитовое ИЛИ*:

`x = x | (1 << n)` + 
(устанавливает n-й бит в 1)

== Как сбросить бит?

Используется *побитовое И с отрицанием*:

`x = x & ~(1 << n)` + 
(устанавливает n-й бит в 0)

== Как переключить бит?

Применяется *побитовое XOR*:

`x = x ^ (1 << n)` +
(меняет n-й бит с 0 на 1 или наоборот)

== Операция унарного + и -

В C++:

* *Унарный +*  делает integer_promotion.
* *Унарный -* меняет знак числа (-x).

== Почему 17000000 + 1 == 17000000 для float?

Число 17000000 (1.7e7) в float представляется с точностью около 7-8 значащих цифр. Когда к нему прибавляют 1, разница слишком мала по сравнению с величиной числа, и значение не меняется из-за ограниченной точности представления float.

== Вывод по отчету

В ходе выполнения данной работы я изучил основные типы данных, используемые в программировании для микроконтроллеров, включая их размеры и особенности представления в памяти. Рассмотрел различные модификаторы типов данных и правила их преобразования как на уровне компилятора, так и при явном указании программиста. 